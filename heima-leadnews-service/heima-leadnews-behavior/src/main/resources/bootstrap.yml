server:
  port: 51502
spring:
  application:
    name: leadnews-behavior
  cloud:
    nacos:
      config:
        file-extension: yml
        server-addr: localhost:8848
      discovery:
        server-addr: localhost:8848